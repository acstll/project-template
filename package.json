{
  "name": "project-www",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "css": "postcss -u postcss-import -u postcss-cssnext -o OUTPUT ./styles/main.css",
    "css:watch": "watch 'npm run css' ./styles",
    "bf": "browserify ./scripts/main.js > OUTPUT",
    "wf": "watchify ./scripts/main.js -o OUTPUT",
    "uglify": "uglifyjs INPUT -mc --preamble '/*! :·) */' -o OUTPUT",
    "disc": "browserify ./scripts/main.js --full-paths | discify --open",
    "lint": "standard scripts/*"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "disc": "^1.3.2",
    "postcss": "^5.0.18",
    "postcss-cli": "^2.5.1",
    "postcss-cssnext": "^2.4.0",
    "postcss-import": "^8.0.2",
    "standard": "^6.0.7",
    "uglifyjs": "^2.4.10",
    "watch": "^0.17.0",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "normalize.css": "^3.0.3"
  }
}
