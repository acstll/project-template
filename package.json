{
  "name": "project-www",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "css": "postcss -u postcss-import -u postcss-cssnext -o OUTPUT ./styles/main.css",
    "css:watch": "watch 'npm run css' ./styles",
    "css:nano": "postcss -u postcss-import -u postcss-cssnext -u cssnano -o OUTPUT ./styles/main.css",
    "bf": "browserify -t babelify ./scripts/main.js > OUTPUT",
    "wf": "watchify ./scripts/main.js -t babelify -v -o OUTPUT",
    "uglify": "uglifyjs INPUT -mc --preamble '/*! :·) */' -o OUTPUT",
    "disc": "browserify -t babelify ./scripts/main.js --full-paths | discify --open",
    "lint": "standard 'scripts/**/*.js'"
  },
  "devDependencies": {
    "babel-plugin-transform-es2015-modules-commonjs": "latest",
    "babel-preset-es2040": "latest",
    "babelify": "latest",
    "browserify": "latest",
    "cssnano": "latest",
    "disc": "latest",
    "postcss": "latest",
    "postcss-cli": "latest",
    "postcss-cssnext": "latest",
    "postcss-import": "latest",
    "standard": "latest",
    "uglifyjs": "latest",
    "watch": "latest",
    "watchify": "latest"
  },
  "dependencies": {
    "normalize.css": "latest"
  }
}
